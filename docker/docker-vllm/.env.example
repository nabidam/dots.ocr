# vLLM Configuration for dots.ocr
# MODEL_PATH can be a Hugging Face repo (e.g. rednote-hilab/dots.mocr) or local path (e.g. /workspace/weights)
MODEL_PATH=rednote-hilab/dots.mocr
SERVED_MODEL_NAME=model
VLLM_PORT=8000
GPU_MEMORY_UTILIZATION=0.9
TENSOR_PARALLEL_SIZE=1
CUDA_VISIBLE_DEVICES=0
